esphome:
  name: jbd-can-bridge

esp32:
  board: esp32dev
  framework:
    type: arduino

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: "jbd-can-bridge-fallback"
    password: "configmode"

captive_portal:

web_server:
  port: 80

logger:

api:

ota:
  - platform: esphome
    password: "ota-password"
